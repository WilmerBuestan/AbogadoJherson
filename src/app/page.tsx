import { PublicLayout } from '@/components/templates/PublicLayout';

export default function Home() {
  return (
    <PublicLayout>
      {/* Todo el contenido público se renderiza aquí adentro */}
    </PublicLayout>
  );
}